// Q 12: Greetings: Start with the array you used in Exercise 11, but instead of 
// just printing each person’s name, print a message to them. The text of each 
// message should be the same, but each message should be personalized with the 
// person’s name.

let names2 : string[] = ['isra', 'irha', 'zainab', 'fatima', 'anaya'];

for (let i=0; i<names2.length; i++){
    console.log(names2[i],'would you like to do combine studies.' )
};